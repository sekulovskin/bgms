CXX_STD = CXX20

PKG_CPPFLAGS = \
  $(shell "$(R_HOME)\bin\Rscript.exe" -e "cat(RcppParallel::CxxFlags())")

PKG_LIBS = \
  $(LAPACK_LIBS) \
  $(BLAS_LIBS)  \
  $(FLIBS)      \
  $(shell "$(R_HOME)\bin\Rscript.exe" -e "cat(RcppParallel::LdFlags())")